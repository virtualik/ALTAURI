Спектр элементов ALTAURI: Классификация и примеры
Давай систематизируем все возможные типы элементов и их "предел дробления".
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ТРИАДА АТОМА                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                           ┌─────────────────┐                               │
│                           │      АТОМ       │                               │
│                           │   (Сущность)    │                               │
│                           └────────┬────────┘                               │
│                                    │                                        │
│            ┌───────────────────────┼───────────────────────┐                │
│            │                       │                       │                │
│            ▼                       ▼                       ▼                │
│   ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│   │       А)        │    │       Б)        │    │       В)        │         │
│   │   COMPUTE       │    │    DATABANK     │    │      FACE       │         │
│   │    MODULE       │    │                 │    │   (DeviceView)  │         │
│   ├─────────────────┤    ├─────────────────┤    ├─────────────────┤         │
│   │                 │    │                 │    │                 │         │
│   │ • onCalculate() │    │ • _buffer[]     │    │ • ЕДИНСТВЕННЫЙ  │         │
│   │ • _process()    │    │ • _state        │    │   экземпляр     │         │
│   │ • onContact     │    │ • _variables    │    │                 │         │
│   │   Changed()     │    │ • _history      │    │ • Может жить    │         │
│   │                 │    │                 │    │   в NodeView    │         │
│   │ • Входы/Выходы  │    │ • Persistable   │    │   ИЛИ           │         │
│   │   обрабатываются│    │   через         │    │   DeviceWindow  │         │
│   │   автоматически │    │   getPersistent │    │                 │         │
│   │                 │    │   State()       │    │ • Перемещается  │         │
│   │ • Активен через │    │                 │    │   по atom.id    │         │
│   │   DriverManager │    │ • Headless:     │    │                 │         │
│   │   (если нужен)  │    │   работает      │    │ • Если создан   │         │
│   │                 │    │   автономно     │    │   для схемы -   │         │
│   │                 │    │                 │    │   переиспользуем│         │
│   └─────────────────┘    └─────────────────┘    └─────────────────┘         │
│                                                                             │
│   Headless Mode:                                                            │
│   ──────────────                                                            │
│   А) работает ✓        Б) работает ✓       В) не нужен ✗                   │
│                                                                             │
│   Workstation Mode:                                                         │
│   ─────────────────                                                        │
│   А) работает ✓        Б) работает ✓       В) создаётся один раз ✓         │
│                                                                             │
│   DeviceWindow открывается:                                                 │
│   ──────────────────────────                                                │
│   "Дай мне лицо атома с id=X"                                               │
│   → Если виджет существует (в NodeView) → забираем его, показываем          │
│   → Если виджет не существует → создаём, привязываем к атому                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
или так:
 * Архитектура "Atom is Databank & Compute Core":
 * ┌─────────────────────────────────────────────────────────────────────────┐
 * │                         АТОМ (Сущность)                                 │
 * │                              │                                          │
 * │        ┌─────────────────────┼─────────────────────┐                    │
 * │        │                     │                     │                    │
 * │        ▼                     ▼                     ▼                    │
 * │   А) COMPUTE            Б) DATABANK           В) FACE                   │
 * │   (вычисления)          (данные)              (DeviceView)              │
 * │                                                 │                       │
 * │                                                 │                       │
 * │                      ┌──────────────────────────┘                       │
 * │                      │                                                  │
 * │                      ▼                                                  │
 * │              DeviceViewRegistry                                         │
 * │              (гарантирует единственность)                               │
 * │                      │                                                  │
 * │         ┌────────────┴────────────┐                                     │
 * │         │                         │                                     │
 * │         ▼                         ▼                                     │
 * │    NodeView (preview)      DeviceWindow (full)                         │
 * │    scaled=0.6              scaled=1.0                                   │
 * │                                                                         │
 * │    Виджет ПЕРЕМЕЩАЕТСЯ между контейнерами,                              │
 * │    но ВСЕГДА один экземпляр на атом.                                    │
 * └─────────────────────────────────────────────────────────────────────────┘
 *
 * Принципы:
 * 1. Один атом = один DeviceView (Лицо)
 * 2. DeviceView может жить в NodeView ИЛИ в DeviceWindow
 * 3. При перемещении - тот же объект, только меняется parent и scale
 * 4. При удалении атома - виджет удаляется из реестра
 *
 * Headless Mode:
 * - Registry не создаёт виджеты автоматически
 * - Виджеты создаются только при запросе от UI
 * - Атомы работают независимо от наличия виджетов


Фундаментальное разделение
┌─────────────────────────────────────────────────────────────────────────┐
│                          ТИПЫ АТОМОВ                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────┐     ┌──────────────────┐     ┌─────────────────┐  │
│  │    DRIVERS       │     │    NATIVE        │     │    CUSTOM       │  │
│  │  (Active Atoms)  │     │    ATOMS         │     │   ASSEMBLIES    │  │
│  │                  │     │  (Pure Logic)    │     │  (Composed)     │  │
│  │  • update(dt)    │     │                  │     │                 │  │
│  │  • isActive=true │     │  • logic func    │     │  • internalAtoms│  │
│  │  • OS bridge     │     │  • stateless     │     │  • connections  │  │
│  └────────┬─────────┘     └────────┬─────────┘     └────────┬────────┘  │
│           │                        │                        │           │
│           ▼                        ▼                        ▼           │
│    Внешний мир               Внутренний мир           Иерархия          │
│    (OS, Hardware)           (Вычисления)            (Композиция)        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

ГРУППА 1: ДРАЙВЕРЫ (Active Atoms)
Суть: Мост между ALTAURI и внешним миром. Имеют update(dt) цикл.

1.1 UI Widgets (HMI - Human Machine Interface)

Button (Кнопка)
┌──────────────────────────────────────┐
│           BUTTON DRIVER              │
│                                      │
│   [OS: Visual Button Component]      │
│              ▲                       │
│              │ mouse events          │
│              ▼                       │
│   ┌─────────────────────────┐        │
│   │  Properties:            │        │
│   │  • label: String        │        │
│   │  • x, y: Position       │        │
│   │  • width, height: Size  │        │
│   │  • color: Int           │        │
│   │  • enabled: Bool        │        │
│   └─────────────────────────┘        │
│                                      │
│   OUTPUTS (внутрь системы):          │
│   ┌─────────┐  ┌──────────┐          │
│   │ pressed │  │ released │          │
│   │ (Bool)  │  │  (Bool)  │          │
│   └─────────┘  └──────────┘          │
│   ┌─────────┐  ┌──────────┐          │
│   │ clicked │  │ holdTime │          │
│   │ (Pulse) │  │  (Float) │          │
│   └─────────┘  └──────────┘          │
└──────────────────────────────────────┘

TextInput (Поле ввода текста)
┌──────────────────────────────────────┐
│         TEXT INPUT DRIVER            │
│                                      │
│   [OS: Input Field Component]        │
│                                      │
│   Properties:                        │
│   • placeholder: String              │
│   • maxLength: Int                   │
│   • readOnly: Bool                   │
│   • fontSize: Int                    │
│   • textColor: Int                   │
│   • backgroundColor: Int             │
│   • x, y, width, height              │
│                                      │
│   INPUTS (из системы):               │
│   ┌─────────┐  ┌──────────┐          │
│   │  text   │  │  focus   │          │
│   │ (String)│  │  (Bool)  │          │
│   └─────────┘  └──────────┘          │
│   ┌─────────┐  ┌──────────┐          │
│   │ enabled │  │  select  │          │
│   │ (Bool)  │  │ (Range)  │          │
│   └─────────┘  └──────────┘          │
│                                      │
│   OUTPUTS (в систему):               │
│   ┌─────────┐  ┌──────────┐          │
│   │  text   │  │ changed  │          │
│   │ (String)│  │ (Pulse)  │          │
│   └─────────┘  └──────────┘          │
│   ┌─────────┐  ┌──────────┐          │
│   │ enterKey│  │  focused │          │
│   │ (Pulse) │  │  (Bool)  │          │
│   └─────────┘  └──────────┘          │
└──────────────────────────────────────┘

TextArea (Многострочный текст)
┌──────────────────────────────────────┐
│         TEXT AREA DRIVER             │
│                                      │
│   [OS: Scrollable Text Display]      │
│                                      │
│   Properties:                        │
│   • editable: Bool                   │
│   • wordWrap: Bool                   │
│   • scrollBars: Bool                 │
│   • font, fontSize, color            │
│   • x, y, width, height              │
│                                      │
│   INPUTS:                            │
│   text (String), append (String)     │
│   clear (Pulse), scroll (Int)        │
│                                      │
│   OUTPUTS:                           │
│   text (String), lineCount (Int)     │
│   selection (String)                 │
└──────────────────────────────────────┘

Slider (Ползунок)
┌──────────────────────────────────────┐
│           SLIDER DRIVER              │
│                                      │
│   [OS: Slider Component]             │
│                                      │
│   Properties:                        │
│   • min: Float                       │
│   • max: Float                       │
│   • step: Float                      │
│   • orientation: Horizontal/Vertical │
│   • x, y, width, height              │
│                                      │
│   INPUTS:  value (Float)             │
│   OUTPUTS: value (Float)             │
│             changed (Pulse)          │
└──────────────────────────────────────┘

CheckBox / Toggle
INPUTS:  checked (Bool), enabled (Bool)
OUTPUTS: checked (Bool), changed (Pulse)
Properties: label, color, x, y
DropDown / Selector
text

INPUTS:  options (Array<String>), selected (Int)
OUTPUTS: selected (Int), selectedText (String), changed (Pulse)
Properties: placeholder, x, y, width
ColorPicker
text

INPUTS:  color (Int), alpha (Float)
OUTPUTS: color (Int), hex (String), rgb ({r,g,b}), changed (Pulse)
Properties: showAlpha, x, y
1.2 Display Widgets (Только отображение)
Label (Текстовая метка)
text

INPUTS: text (String), color (Int), visible (Bool)
Properties: fontSize, alignment, autoSize, x, y
ProgressBar
text

INPUTS: value (Float 0-1), text (String), color (Int)
Properties: showText, x, y, width, height
LED / Indicator
text

INPUTS: on (Bool), color (Int), blink (Bool)
Properties: onColor, offColor, x, y, radius
Graph / Chart
text

INPUTS: addPoint ({x, y}), clear (Pulse), autoScale (Bool)
OUTPUTS: visibleRange ({min, max})
Properties: lineColor, lineWidth, fillArea, x, y, width, height
Gauge / Dial
text

INPUTS: value (Float), min (Float), max (Float)
Properties: units, showValue, needleColor, x, y, radius
1.3 Hardware Drivers (Железо)

SerialPort (COM Port)
┌──────────────────────────────────────┐
│         SERIAL PORT DRIVER           │
│                                      │
│   [OS: Serial Communication]         │
│                                      │
│   Properties:                        │
│   • portName: String ("COM1", ...)   │
│   • baudRate: Int (9600, 115200...)  │
│   • dataBits: Int (7, 8)             │
│   • parity: None/Odd/Even            │
│   • stopBits: Int (1, 2)             │
│                                      │
│   INPUTS:                            │
│   ┌─────────┐  ┌──────────┐          │
│   │ txData  │  │  open    │          │
│   │ (Bytes) │  │ (Pulse)  │          │
│   └─────────┘  └──────────┘          │
│   ┌─────────┐  ┌──────────┐          │
│   │ txText  │  │  close   │          │
│   │ (String)│  │ (Pulse)  │          │
│   └─────────┘  └──────────┘          │
│                                      │
│   OUTPUTS:                           │
│   ┌─────────┐  ┌──────────┐          │
│   │ rxData  │  │ rxText   │          │
│   │ (Bytes) │  │ (String) │          │
│   └─────────┘  └──────────┘          │
│   ┌─────────┐  ┌──────────┐          │
│   │ isOpen  │  │  error   │          │
│   │ (Bool)  │  │ (String) │          │
│   └─────────┘  └──────────┘          │
└──────────────────────────────────────┘

TCP/UDP Socket

INPUTS:  send (Bytes/String), connect (Pulse), disconnect (Pulse)
OUTPUTS: received (Bytes), connected (Bool), error (String)
Properties: host, port, protocol (TCP/UDP), timeout
File Watcher
text

INPUTS:  path (String), start (Pulse), stop (Pulse)
OUTPUTS: changed (Pulse), content (String), exists (Bool)
Properties: pollInterval, encoding
File Reader/Writer
text

FileReader:
  INPUTS: path (String), read (Pulse)
  OUTPUTS: content (String/Bytes), success (Bool), error (String)

FileWriter:
  INPUTS: path (String), content (String/Bytes), write (Pulse), append (Bool)
  OUTPUTS: success (Bool), error (String), bytesWritten (Int)
MIDI Device
text

INPUTS:  noteOn (channel, note, velocity), noteOff, controlChange
OUTPUTS: noteOn, noteOff, controlChange, clock
Properties: deviceName, deviceIndex
Audio Output
text

INPUTS:  sample (Float), frequency (Float), play (Pulse), stop (Pulse)
OUTPUTS: playing (Bool), position (Float)
Properties: sampleRate, channels, buffer
GPIO (Raspberry Pi / Embedded)
text

DigitalInput:  OUTPUT: value (Bool), changed (Pulse)
DigitalOutput: INPUT: value (Bool)
AnalogInput:   OUTPUT: value (Float 0-1)
PWMOutput:     INPUT: dutyCycle (Float 0-1), frequency (Float)
Properties: pin, pullUp, pullDown, debounce
1.4 System Drivers

Timer
┌──────────────────────────────────────┐
│           TIMER DRIVER               │
│                                      │
│   [System: High Precision Timer]     │
│                                      │
│   INPUTS:                            │
│   interval (Float ms), start (Pulse) │
│   stop (Pulse), repeat (Bool)        │
│                                      │
│   OUTPUTS:                           │
│   tick (Pulse), count (Int)          │
│   elapsed (Float), running (Bool)    │
└──────────────────────────────────────┘

Clock (Real Time)
INPUTS:  format (String)
OUTPUTS: timestamp (Float), formatted (String), date ({y,m,d}), time ({h,m,s})
Properties: timezone, format
System Info

OUTPUTS: cpuUsage (Float), memoryUsage (Int), fps (Float)
         screenWidth (Int), screenHeight (Int), osName (String)
Properties: updateInterval
Keyboard

OUTPUTS: keyPressed (Int), keyReleased (Int), keyChar (String)
         modifiers ({shift, ctrl, alt, meta})
Properties: captureGlobal (Bool)
Mouse

OUTPUTS: x (Float), y (Float), deltaX (Float), deltaY (Float)
         leftBtn (Bool), rightBtn (Bool), middleBtn (Bool)
         scroll (Float)
Properties: relativeMode (Bool), capture (Bool)
Clipboard

INPUTS:  setText (String), getText (Pulse)
OUTPUTS: text (String), hasText (Bool)
HTTP Client

INPUTS:  url (String), method (GET/POST), body (String), headers (Map)
         request (Pulse)
OUTPUTS: response (String), statusCode (Int), headers (Map), error (String)
Properties: timeout, followRedirects
Process Launcher

INPUTS:  command (String), args (Array), start (Pulse), kill (Pulse)
OUTPUTS: stdout (String), stderr (String), exitCode (Int), running (Bool)
Properties: workingDir, env (Map), captureOutput
ГРУППА 2: NATIVE ATOMS (Pure Logic)
Суть: Stateless вычислители. logic: Array<Dynamic> → Array<Dynamic>

2.1 Математические
┌─────────────────────────────────────────────────────────────┐
│                    MATH NATIVE ATOMS                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ARITHMETIC                                                 │
│  ──────────                                                 │
│  Add:      [a, b] → [a + b]                                 │
│  Subtract: [a, b] → [a - b]                                 │
│  Multiply: [a, b] → [a * b]                                 │
│  Divide:   [a, b] → [b != 0 ? a/b : null]                   │
│  Modulo:   [a, b] → [a % b]                                 │
│  Negate:   [a] → [-a]                                       │
│                                                             │
│  COMPARISON                                                 │
│  ──────────                                                 │
│  Equal:    [a, b] → [a == b]                                │
│  NotEqual: [a, b] → [a != b]                                │
│  Greater:  [a, b] → [a > b]                                 │
│  Less:     [a, b] → [a < b]                                 │
│  GreaterEq:[a, b] → [a >= b]                                │
│  LessEq:   [a, b] → [a <= b]                                │
│                                                             │
│  ADVANCED                                                   │
│  ──────────                                                 │
│  Power:    [base, exp] → [Math.pow(base, exp)]              │
│  Sqrt:     [a] → [Math.sqrt(a)]                             │
│  Abs:      [a] → [Math.abs(a)]                              │
│  Round:    [a] → [Math.round(a)]                            │
│  Floor:    [a] → [Math.floor(a)]                            │
│  Ceil:     [a] → [Math.ceil(a)]                             │
│  Min:      [a, b] → [Math.min(a, b)]                        │
│  Max:      [a, b] → [Math.max(a, b)]                        │
│  Clamp:    [val, min, max] → [clamped]                      │
│  Lerp:     [a, b, t] → [a + (b-a)*t]                        │
│                                                             │
│  TRIGONOMETRY                                               │
│  ────────────                                               │
│  Sin:  [rad] → [Math.sin(rad)]                              │
│  Cos:  [rad] → [Math.cos(rad)]                              │
│  Tan:  [rad] → [Math.tan(rad)]                              │
│  Asin: [val] → [Math.asin(val)]                             │
│  Acos: [val] → [Math.acos(val)]                             │
│  Atan2:[y, x] → [Math.atan2(y, x)]                          │
│                                                             │
│  RANDOM                                                     │
│  ──────                                                     │
│  Random:    [] → [Math.random()]                            │
│  RandomInt: [min, max] → [random int in range]              │
│  RandomBool:[probability] → [true/false]                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘

2.2 Логические
┌─────────────────────────────────────────────────────────────┐
│                   LOGIC NATIVE ATOMS                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  GATES                                                      │
│  ─────                                                      │
│  And:  [a, b] → [a && b]                                    │
│  Or:   [a, b] → [a || b]                                    │
│  Not:  [a] → [!a]                                           │
│  Xor:  [a, b] → [a != b]                                    │
│  Nand: [a, b] → [!(a && b)]      ← Universal gate!          │
│  Nor:  [a, b] → [!(a || b)]                                 │
│                                                             │
│  CONDITIONAL                                                │
│  ───────────                                                │
│  Ternary: [condition, ifTrue, ifFalse] → [result]           │
│  Switch:  [value, case1, result1, case2, result2, default]  │
│                                                             │
│  TYPE CHECKS                                                │
│  ───────────                                                │
│  IsNull:    [a] → [a == null]                               │
│  IsNumber:  [a] → [Std.isOfType(a, Float)]                  │
│  IsString:  [a] → [Std.isOfType(a, String)]                 │
│  IsBool:    [a] → [Std.isOfType(a, Bool)]                   │
│  IsArray:   [a] → [Std.isOfType(a, Array)]                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘

2.3 Строковые
┌─────────────────────────────────────────────────────────────┐
│                  STRING NATIVE ATOMS                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  MANIPULATION                                               │
│  ────────────                                               │
│  Concat:   [a, b] → [a + b]                                 │
│  Length:   [s] → [s.length]                                 │
│  CharAt:   [s, index] → [s.charAt(index)]                   │
│  Substring:[s, start, end] → [s.substring(start, end)]      │
│  Split:    [s, delimiter] → [array of parts]                │
│  Join:     [array, delimiter] → [joined string]             │
│  Trim:     [s] → [StringTools.trim(s)]                      │
│  ToUpper:  [s] → [s.toUpperCase()]                          │
│  ToLower:  [s] → [s.toLowerCase()]                          │
│  Replace:  [s,from, to] → [StringTools.replace(s, from, to)]│
│                                                             │
│  SEARCH                                                     │
│  ──────                                                     │
│  Contains: [s, search] → [s.indexOf(search) >= 0]           │
│  IndexOf:  [s, search] → [s.indexOf(search)]                │
│  StartsWith:[s, prefix]→ [StringTools.startsWith(s, prefix)]│
│  EndsWith: [s, suffix] → [StringTools.endsWith(s, suffix)]  │
│                                                             │
│  FORMAT                                                     │
│  ──────                                                     │
│  Format:   [template, ...args] → [formatted string]         │
│  PadLeft:  [s, len, char] → [padded string]                 │
│  PadRight: [s, len, char] → [padded string]                 │
│                                                             │
│  PARSING                                                    │
│  ───────                                                    │
│  ParseInt: [s] → [Std.parseInt(s)]                          │
│  ParseFloat:[s] → [Std.parseFloat(s)]                       │
│  ToString: [a] → [Std.string(a)]                            │
│  ToFixed:  [n, decimals] → [n.toFixed(decimals)]            │
│                                                             │
└─────────────────────────────────────────────────────────────┘

2.4 Массивы и коллекции
┌─────────────────────────────────────────────────────────────┐
│                   ARRAY NATIVE ATOMS                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  CREATION                                                   │
│  ────────                                                   │
│  ArrayCreate: [] → [[]]                                     │
│  ArrayFrom:   [...elements] → [array]                       │
│  Range:       [start, end, step] → [array of numbers]       │
│                                                             │
│  ACCESS                                                     │
│  ──────                                                     │
│  Get:    [arr, index] → [arr[index]]                        │
│  Set:    [arr, index, value] → [modified array]             │
│  First:  [arr] → [arr[0]]                                   │
│  Last:   [arr] → [arr[arr.length-1]]                        │
│  Length: [arr] → [arr.length]                               │
│                                                             │
│  MODIFICATION                                               │
│  ────────────                                               │
│  Push:     [arr, value] → [arr.concat([value])]             │
│  Pop:      [arr] → [arr.slice(0, -1)]                       │
│  Shift:    [arr] → [arr.slice(1)]                           │
│  Unshift:  [arr, value] → [[value].concat(arr)]             │
│  Slice:    [arr, start, end] → [arr.slice(start, end)]      │
│  Splice:   [arr, start, count, ...items] → [modified]       │
│  Reverse:  [arr] → [arr.reverse()]                          │
│  Sort:     [arr, comparator] → [sorted array]               │
│                                                             │
│  SEARCH & FILTER                                            │
│  ────────────────                                           │
│  Contains: [arr, value] → [arr.indexOf(value) >= 0]         │
│  IndexOf:  [arr, value] → [arr.indexOf(value)]              │
│  Filter:   [arr, predicate] → [filtered array]              │
│  Find:     [arr, predicate] → [found element or null]       │
│                                                             │
│  TRANSFORM                                                  │
│  ─────────                                                  │
│  Map:    [arr, func] → [mapped array]                       │
│  Reduce: [arr, func, initial] → [reduced value]             │
│  Flatten:[arr of arrays] → [flat array]                     │
│                                                             │
│  AGGREGATION                                                │
│  ───────────                                                │
│  Sum:    [arr] → [sum of all elements]                      │
│  Avg:    [arr] → [average]                                  │
│  Min:    [arr] → [minimum element]                          │
│  Max:    [arr] → [maximum element]                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘

2.5 Конверсия типов
┌─────────────────────────────────────────────────────────────┐
│                  CONVERSION NATIVE ATOMS                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  TO NUMBER                                                  │
│  ─────────                                                  │
│  StringToNumber: [s] → [Std.parseFloat(s)]                  │
│  BoolToNumber:   [b] → [b ? 1 : 0]                          │
│                                                             │
│  TO STRING                                                  │
│  ─────────                                                  │
│  NumberToString: [n] → [Std.string(n)]                      │
│  BoolToString:   [b] → [b ? "true" : "false"]               │
│  ArrayToString:  [arr] → [Json.stringify(arr)]              │
│                                                             │
│  TO BOOL                                                    │
│  ────────                                                   │
│  NumberToBool: [n] → [n != 0]                               │
│  StringToBool: [s] → [s != "" && s != "false"]              │
│                                                             │
│  ENCODING                                                   │
│  ────────                                                   │
│  EncodeBase64: [s] → [Base64.encode(s)]                     │
│  DecodeBase64: [s] → [Base64.decode(s)]                     │
│  EncodeJSON:   [obj] → [Json.stringify(obj)]                │
│  DecodeJSON:   [s] → [Json.parse(s)]                        │
│  EncodeHex:    [bytes] → [hex string]                       │
│  DecodeHex:    [hex] → [bytes]                              │
│                                                             │
│  BINARY                                                     │
│  ──────                                                     │
│  IntToBytes:    [n, size] → [byte array]                    │
│  BytesToInt:    [bytes] → [integer]                         │
│  FloatToBytes:  [n] → [byte array]                          │
│  BytesToFloat:  [bytes] → [float]                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘

ГРУППА 3: STATEFUL ATOMS (Хранение состояния)
Суть: Не являются Driver-ами (нет update(dt)), но хранят внутреннее состояние.
┌─────────────────────────────────────────────────────────────┐
│                   STATEFUL ATOMS                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  STORAGE                                                    │
│  ───────                                                    │
│  ┌────────────────────────────────────────────┐             │
│  │  VARIABLE / REGISTER                       │             │
│  │  INPUTS:  set (value), reset (Pulse)       │             │
│  │  OUTPUTS: value                            │             │
│  │  Properties: defaultValue, persistent      │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  ACCUMULATOR                               │             │
│  │  INPUTS:  add (value), subtract (value)    │             │
│  │           multiply (value), reset (Pulse)  │             │
│  │  OUTPUTS: value                            │             │
│  │  Properties: initialValue                  │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  COUNTER                                   │             │
│  │  INPUTS:  increment (Pulse), decrement     │             │
│  │           set (Int), reset (Pulse)         │             │
│  │  OUTPUTS: value (Int), overflow (Bool)     │             │
│  │  Properties: min, max, wrap (Bool)         │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  SHIFT REGISTER                            │             │
│  │  INPUTS:  push (value), clear (Pulse)      │             │
│  │  OUTPUTS: value0, value1, ..., valueN      │             │
│  │  Properties: depth (Int)                   │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  FIFO BUFFER / QUEUE                       │             │
│  │  INPUTS:  push (value), pop (Pulse)        │             │
│  │           clear (Pulse)                    │             │
│  │  OUTPUTS: front, back, size, isEmpty       │             │
│  │  Properties: maxSize                       │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  LIFO BUFFER / STACK                       │             │
│  │  INPUTS:  push (value), pop (Pulse)        │             │
│  │  OUTPUTS: top, size, isEmpty               │             │
│  │  Properties: maxSize                       │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  HISTORY / RECORDER                        │             │
│  │  INPUTS:  record (value), play (Pulse)     │             │
│  │           clear (Pulse)                    │             │
│  │  OUTPUTS: value, index, length             │             │
│  │  Properties: maxSize, loop (Bool)          │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
└─────────────────────────────────────────────────────────────┘

ГРУППА 4: FLOW CONTROL (Управление потоком)
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  MERGE / MUX                               │             │
│  │  INPUTS:  selector (Int), in0, in1, ...inN │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Logic: out = in[selector]                 │             │
│  │  Properties: inputCount                    │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  GATING                                                     │
│  ──────                                                     │
│  ┌────────────────────────────────────────────┐             │
│  │  GATE (Enable/Disable)                     │             │
│  │  INPUTS:  value (Any), enable (Bool)       │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Logic: Если enable=true, value→out        │             │
│  │         Иначе out не меняется              │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  LATCH                                     │             │
│  │  INPUTS:  value (Any), latch (Pulse)       │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Logic: value→out только при latch импульсе│             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  SAMPLE & HOLD                             │             │
│  │  INPUTS:  value (Any), sample (Pulse)      │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Logic: Запоминает value при sample        │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  DELAYS                                                     │
│  ──────                                                     │
│  ┌────────────────────────────────────────────┐             │
│  │  DELAY LINE                                │             │
│  │  INPUTS:  value (Any)                      │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Properties: delayFrames (Int)             │             │
│  │  Logic: Выход = вход N кадров назад        │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  ONE-SHOT / PULSE                          │             │
│  │  INPUTS:  trigger (Any)                    │             │
│  │  OUTPUTS: pulse (Pulse)                    │             │
│  │  Logic: Любой input→единичный импульс      │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  EDGE DETECTOR                             │             │
│  │  INPUTS:  signal (Bool)                    │             │
│  │  OUTPUTS: rising (Pulse), falling (Pulse)  │             │
│  │  Logic: Детектирует переходы 0→1 и 1→0     │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  THROTTLING                                                 │
│  ──────────                                                 │
│  ┌────────────────────────────────────────────┐             │
│  │  THROTTLE                                  │             │
│  │  INPUTS:  value (Any)                      │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Properties: minInterval (ms)              │             │
│  │  Logic: Пропускает не чаще interval        │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
│  ┌────────────────────────────────────────────┐             │
│  │  DEBOUNCE                                  │             │
│  │  INPUTS:  value (Any)                      │             │
│  │  OUTPUTS: out (Any)                        │             │
│  │  Properties: delay (ms)                    │             │
│  │  Logic: Передаёт после паузы в изменениях  │             │
│  └────────────────────────────────────────────┘             │
│                                                             │
└─────────────────────────────────────────────────────────────┘

ГРУППА 5: SPECIAL / UTILITY
┌─────────────────────────────────────────────────────────────┐
│                   SPECIAL ATOMS                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  DEBUGGING                                                  │
│  ─────────                                                  │
│  Log:      [value] → [value] (side-effect: trace)           │
│  Breakpoint:[value] → [value] (side-effect: debug break)    │
│  Watch:    [value] → [] (displays in debug panel)           │
│                                                             │
│  CONSTANTS                                                  │
│  ─────────                                                  │
│  ConstNumber: [] → [value] (Properties: value)              │
│  ConstString: [] → [value] (Properties: value)              │
│  ConstBool:   [] → [value] (Properties: value)              │
│                                                             │
│  GENERATORS                                                 │
│  ──────────                                                 │
│  PulseGenerator: [] → [pulse] (Properties: interval)        │
│  SineWave: [] → [value] (Properties: freq, amp, phase)      │
│  NoiseGenerator: [] → [value] (Properties: type, seed)      │
│                                                             │
│  ROUTING                                                    │
│  ────────                                                   │
│  Pass:    [in] → [out] (Identity - useful for routing)      │
│  Splitter:[value] → [out1, out2, out3, ...]                 │
│  Merger:  [in1, in2, ...] → [array]                         │
│                                                             │
│  NULL HANDLING                                              │
│  ──────────────                                             │
│  NullCoalesce: [value, fallback] → [value ?? fallback]      │
│  NullGuard:    [value] → [value] (выход только если не null)│
│                                                             │
│  PROPERTIES ACCESS                                          │
│  ────────────────                                           │
│  GetProperty: [object, key] → [object[key]]                 │
│  SetProperty: [object, key, value] → [modified object]      │
│                                                             │
└─────────────────────────────────────────────────────────────┘

Итоговая классификация библиотеки

ALTAURI LIBRARY
│   │   ├── Audio
│   │   ├── GPIO
│   │   └── ...
│   └── System
│       ├── Timer
│       ├── Clock
│       ├── Keyboard
│       ├── Mouse
│       ├── Clipboard
│       ├── FileIO
│       ├── HTTP
│       └── ...
│
├── 02_LOGIC (Native, Stateless)
│   ├── Math
│   │   ├── Arithmetic (+, -, *, /, %)
│   │   ├── Comparison (==, !=, >, <, >=, <=)
│   │   ├── Advanced (pow, sqrt, abs, round...)
│   │   ├── Trig (sin, cos, tan...)
│   │   └── Random
│   ├── Boolean
│   │   ├── Gates (AND, OR, NOT, XOR, NAND, NOR)
│   │   └── Conditional (Ternary, Switch)
│   ├── String
│   │   ├── Manipulation (concat, split, replace...)
│   │   ├── Search (contains, indexOf...)
│   │   └── Format (toUpperCase, trim...)
│   ├── Array
│   │   ├── Access (get, set, slice...)
│   │   ├── Modify (push, pop, filter...)
│   │   └── Aggregate (sum, avg, min, max...)
│   └── Conversion
│       ├── Type (toInt, toString, toBool...)
│       └── Encoding (Base64, JSON, Hex...)
│
├── 03_STATE (Stateful)
│   ├── Storage
│   │   ├── Variable
│   │   ├── Accumulator
│   │   └── Counter
│   ├── Buffer
│   │   ├── ShiftRegister
│   │   ├── FIFO (Queue)
│   │   ├── LIFO (Stack)
│   │   └── History
│   └── ...
│
├── 04_FLOW (Control)
│   ├── Branching
│   │   ├── If
│   │   ├── Switch (Demux)
│   │   └── Merge (Mux)
│   ├── Gating
│   │   ├── Gate
│   │   ├── Latch
│   │   └── SampleHold
│   ├── Timing
│   │   ├── Delay
│   │   ├── EdgeDetector
│   │   ├── Throttle
│   │   └── Debounce
│   └── Pulse
│       ├── OneShot
│       └── ...
│
└── 05_SPECIAL
    ├── Debug (Log, Watch, Breakpoint)
    ├── Constants
    ├── Generators
    └── Routing (Pass, Split, Merge)
	